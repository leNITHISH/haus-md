# Haus MD

A *House, M.D.* appreciation project — and an excuse to learn the fundamentals
of data engineering with [DuckDB](https://duckdb.org/).

The idea: pull the show's data out of the full IMDb dataset dump and build up
a small local warehouse (`haus.duckdb`) one query at a time, learning DuckDB's
SQL dialect and its `read_csv` ingestion options along the way.

**Status: on hold** (uni exams). See [`notes/progress.md`](notes/progress.md)
for exactly where things were left off and what to pick up next.

## Project structure

```
.
├── explore.py         # scratch script — DuckDB queries run so far, oldest at top (commented out)
├── data/               # raw IMDb .tsv.gz dumps (gitignored, not committed — see Setup)
├── haus.duckdb         # local DuckDB database file (gitignored, generated by explore.py)
├── notes/
│   ├── duck.md         # DuckDB syntax/gotchas learned so far
│   └── progress.md     # running log: what's done, what's next, open bugs
└── requirements.txt
```

## Data source

The raw files are the official IMDb non-commercial datasets
(`title.basics.tsv.gz`, `title.episode.tsv.gz`, `title.ratings.tsv.gz`) from
https://datasets.imdbws.com/. They're large (~280 MB total) and gitignored,
so they aren't committed — download them yourself and drop them in `data/`.

## Setup

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

mkdir -p data
# download title.basics.tsv.gz, title.episode.tsv.gz, title.ratings.tsv.gz
# from https://datasets.imdbws.com/ into data/

python3 explore.py
```

`explore.py` connects to (and creates, if missing) `haus.duckdb` in the repo
root and runs whatever query block is currently uncommented at the bottom of
the file.

## Approach so far

Working title-by-title, filtered down from the full IMDb dump to just
*House, M.D.* (`tt0412142`, `startYear = 2004`):

1. `house_basics` — the show's own row from `title.basics`.
2. `house_episodes` — all episodes whose `parentTconst` matches the show,
   pulled from `title.episode`.
3. `house_ratings` — in progress, see progress notes.

Each step was built by first `DESCRIBE`-ing the raw CSV to check DuckDB's
inferred schema, then `CREATE TABLE ... AS SELECT` to materialize the
filtered slice.

## Learnings

DuckDB-specific notes (options like `nullstr`/`sample_size`, gotchas hit
along the way) are kept in [`notes/duck.md`](notes/duck.md) as they come up.
